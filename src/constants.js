const api = 'http://localhost:3001';
const client = 'http://localhost:3000';
const validAuth = ['local', 'facebook'];
const validGroupTypes = ['oligarchy', 'democracy'];

export {
  api,
  client,
  validAuth,
  validGroupTypes
};
