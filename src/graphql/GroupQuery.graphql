#import "./GroupListFragment.graphql"

query Group($key: String!) {
    group(key: $key) {
        ...GroupFragment,
        description,
        groupUsers {
            _id
            _key
            type
            user {
                _id
            }
        }
    }
}
